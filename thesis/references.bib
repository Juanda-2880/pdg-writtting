% Shared BibTeX database for the thesis (see ../latex/references/citations-and-figures.md).
%
% Every \citet{}/\citep{} key used anywhere under chapters/ must have a real
% entry here. Never invent a placeholder entry to make a build pass — use
% the literal key `cite_needed` as a visible, grep-able marker instead (see
% ../agent-roles/Investigador.md), e.g. \citep{cite_needed}, and add the
% real entry once the Investigador supplies a source.
